import { ClickStopPropaginationDirective } from './click-stop-propagination.directive';

describe('ClickStopPropaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickStopPropaginationDirective();
    expect(directive).toBeTruthy();
  });
});
